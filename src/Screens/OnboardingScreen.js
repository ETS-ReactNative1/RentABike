import { View, Text } from 'react-native';
import React from 'react';
import { OnOnboarding } from '../Components/Onboarding';

export function OnboardingScreen({ navigation }) {
  return <OnOnboarding navigation={navigation} />;
}
